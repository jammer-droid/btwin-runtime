[project]
name = "btwin-cli"
version = "0.1.0"
description = "CLI control plane for the B-TWIN collaboration runtime"
requires-python = ">=3.11"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/btwin_cli"]
